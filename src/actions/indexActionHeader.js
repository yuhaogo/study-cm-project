// import {cFetch} from '../util/cFetch';
// import cookie from 'js-cookie';

let actions ={
    changeNavs: activeCode =>({
        type:'INDEX_CHANGE_NAV',
        payload:activeCode
    })
}
export default actions;

    